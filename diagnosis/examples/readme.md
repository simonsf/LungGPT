You can choose your own training framework and testing framework, and the data formats in this project can be used directly.

This project provides an API method for prediction. The specific operation steps are as follows:
1. Modify the API-related content and model hyperparameters in the config.
2. Modify the download path of this project in example/api.py.
3. Run the following commands.
`python ./example/api.py`